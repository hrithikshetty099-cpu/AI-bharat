import { useState } from 'react'

export default function TravellerDetails({ user, onContinue, onBack }) {
  const [form, setForm] = useState({ home: '', language: 'English', style: 'Balanced', interests: ['Nature'] })
  const interests = ['Nature', 'History', 'Food', 'Adventure', 'Photography']
  const toggle = (interest) => setForm((current) => ({ ...current, interests: current.interests.includes(interest) ? current.interests.filter((item) => item !== interest) : [...current.interests, interest] }))
  return <main className="auth-page"><header className="subpage-nav"><button className="site-brand" onClick={onBack}><span>✦</span><strong>BharatTrails <i>AI</i></strong></button><span className="onboarding-step">02 / 03 · TRAVELLER DETAILS</span></header><section className="traveller-layout"><div><p className="home-eyebrow">NICE TO MEET YOU, {user?.name?.toUpperCase()}</p><h1>Tell us how<br /><em>you travel.</em></h1><p>These preferences stay with your profile and make every India recommendation more personal.</p></div><form className="auth-card" onSubmit={(event) => { event.preventDefault(); onContinue({ ...user, ...form }) }}><h2>Traveller preferences</h2><label>Home city<input value={form.home} onChange={(event) => setForm({ ...form, home: event.target.value })} placeholder="Mangaluru" /></label><label>Preferred language<select value={form.language} onChange={(event) => setForm({ ...form, language: event.target.value })}><option>English</option><option>Hindi</option><option>Kannada</option></select></label><label>Travel style<select value={form.style} onChange={(event) => setForm({ ...form, style: event.target.value })}><option>Balanced</option><option>Slow and local</option><option>Adventure first</option><option>Heritage trail</option></select></label><div><label>Interests</label><div className="traveller-interests">{interests.map((interest) => <button type="button" className={form.interests.includes(interest) ? 'selected' : ''} key={interest} onClick={() => toggle(interest)}>{interest}</button>)}</div></div><button className="primary-action" type="submit">Explore India <span>→</span></button></form></section></main>
}
