export default function TripReport({ itinerary, tripData, booking, onBack, onHome }) {
  const days = itinerary?.dailyPlans?.length || Number(tripData?.days || 1)
  const budget = Number(tripData?.budget || itinerary?.budget || 0)
  const spent = Number(tripData?.busCost || 0) + Number(itinerary?.totalEstimatedCost || 0)
  return <main className="report-page"><header className="subpage-nav"><button className="site-brand" onClick={onHome}><span>✦</span><strong>BharatTrails <i>AI</i></strong></button><button className="back-link" onClick={onBack}>← Live trip</button></header><section className="report-hero"><p className="home-eyebrow">TRIP COMPLETE · BHARATTRAILS REPORT</p><h1>Your journey,<br /><em>well remembered.</em></h1><p>{itinerary?.destination || tripData?.destination || 'India'} · {days} day{days === 1 ? '' : 's'} · {tripData?.travellers || 1} traveller{Number(tripData?.travellers || 1) === 1 ? '' : 's'}</p></section><section className="report-grid"><article><small>PLACES EXPLORED</small><strong>{itinerary?.dailyPlans?.flatMap((day) => day.activities || []).filter((activity) => !activity.isBreak).length || 0}</strong></article><article><small>ESTIMATED SPEND</small><strong>₹{spent.toLocaleString('en-IN')}</strong></article><article><small>BUDGET REMAINING</small><strong>₹{Math.max(0, budget - spent).toLocaleString('en-IN')}</strong></article><article><small>BOOKING</small><strong>{booking?.status || 'Demo trip'}</strong></article></section><section className="report-summary"><p className="home-eyebrow">YOUR ROUTE</p><h2>{itinerary?.summary || 'A considered Indian journey, shaped around your pace and interests.'}</h2><button className="primary-action" onClick={onHome}>Plan another India trip <span>→</span></button></section></main>
}
