export default function BookingConfirmation({ booking, onGenerate, onBack }) {
  return <main className="booking-page"><header className="subpage-nav"><button className="site-brand" onClick={onBack}><span>✦</span><strong>BharatTrails <i>AI</i></strong></button><button className="back-link" onClick={onBack}>← Back to planner</button></header><section className="confirmation-card"><span className="confirmation-mark">✓</span><p className="home-eyebrow">DEMO BOOKING</p><h1>Your journey<br /><em>has a seat.</em></h1><p>Demo Booking — No real payment processed.</p><div className="booking-ticket"><span>{booking.from}</span><b>→</b><span>{booking.to}</span><small>{booking.operator_name} · {booking.departure_time} → {booking.arrival_time}<br />Reference: {booking.booking_id}</small></div><div className="confirmation-actions"><button className="primary-action" onClick={onGenerate}>Generate my itinerary <span>→</span></button><button className="text-action" onClick={() => navigator.share ? navigator.share({ title: 'My BharatTrails booking', text: `${booking.from} to ${booking.to} · ${booking.travel_date}` }) : window.alert('Booking summary ready to share.')}>Share trip ↗</button></div></section></main>
}
